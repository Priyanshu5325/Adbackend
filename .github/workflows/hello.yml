name: My first workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello, GitHub Actions"

      - name: Print Name
        run: echo "Priyanshu Kaliyar"